package mchorse.bbs_mod.ui.dashboard.textures;

/**
 * Active painting tool in the texture pixel editor (brush / eraser / flood fill).
 */
public enum TexturePaintTool
{
    BRUSH,
    ERASER,
    FILL,
    PIPETTE,
    SELECTION
}
